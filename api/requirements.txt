# xgboost is pinned to MATCH the version the margin-v2 bundle was pickled with
# (xgboost does not guarantee cross-version pickle compatibility for the sklearn
# wrapper). numpy/scipy/joblib pins verified to load + predict the bundle
# identically. Revisit these pins only when the model is retrained.
xgboost==2.1.3
numpy==1.23.5
scipy==1.9.3
joblib==1.2.0

fastapi==0.115.6
# Plain uvicorn (not [standard]) — the API sits behind nginx and doesn't need
# uvloop/websockets/watchfiles/httptools (~19MB of extras).
uvicorn==0.34.0
pydantic==2.10.4
# boto3 only — the API reads the track record + current features from S3
# directly. s3fs (and its aiohttp/fsspec/aiobotocore deps) is pipeline-only.
boto3==1.34.95
