# scikit-learn/numpy are pinned to MATCH the environment the model was pickled
# in (cover_classifier.joblib was trained under scikit-learn 1.1.2 / numpy
# 1.20.x on Python 3.8). sklearn does not guarantee cross-version unpickling, so
# we reproduce the serving env to load the artifact safely and get identical
# predictions. Revisit these pins only when the model is retrained.
# Pinned to match the pickled artifact (trained on 1.1.2) so it unpickles with
# ZERO version warnings. 1.1.2 ships cp38/39/310 Linux wheels only — hence the
# python:3.10-slim base in the Dockerfile (no cp311 wheel exists). macOS arm64
# has no 1.1.2 wheel at all; for local dev on a Mac use scikit-learn==1.1.3
# (same 1.1.x minor, identical RF predictions, benign version warning) or Docker.
scikit-learn==1.1.2
numpy==1.23.5
scipy==1.9.3
joblib==1.2.0

fastapi==0.115.6
# Plain uvicorn (not [standard]) — the API sits behind nginx and doesn't need
# uvloop/websockets/watchfiles/httptools (~19MB of extras).
uvicorn==0.34.0
pydantic==2.10.4
# boto3 only — the API reads the model + track record from S3 directly. s3fs (and
# its aiohttp/fsspec/aiobotocore deps) is a pipeline-only dependency, not needed here.
boto3==1.34.95
